---
title: Model aliases
description: Global model-name mapping.
---

Model aliases map a global model name to a target the backend understands. Alias resolution happens in the proxy [transform pipeline](/docs/concepts/proxy) after routing rules, so clients can use stable names regardless of how models are declared in `config.yaml`.
